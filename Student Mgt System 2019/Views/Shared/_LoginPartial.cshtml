@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Manage", "Account", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">

@if ((string)Session["user_type"] == "0")
{
        <li>@Html.ActionLink("Edit Groups", "AdminGroup", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Edit Projects", "AdminProject", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
       <li>@Html.ActionLink("Edit Research Projects", "AdminResearch", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Register", "Register", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
}
else if ((string)Session["user_type"] == "1")
{
    <li>@Html.ActionLink("Profile", "Profile", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
    <li>@Html.ActionLink("Edit Profile Page", "EditProfile2", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
}

       

        @if(Session["user_type"] ==null)
        {
             <li>@Html.ActionLink("Log in", "login", "Home", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        }
        else
        {
            <li>@Html.ActionLink("Log out", "logout", "Home", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        }
       

    </ul>
}
